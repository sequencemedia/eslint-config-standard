import {
  expect
} from 'chai'

import stylistic from '@sequencemedia/eslint-config-standard/config/stylistic'

describe('@sequencemedia/eslint-config-standard/config/stylistic', () => {
  describe('`stylistic`', () => it('is an object', () => expect(stylistic).to.be.an('object')))
})
