import {
  expect
} from 'chai'

import recommended from '@sequencemedia/eslint-config-standard/config/recommended'

describe('@sequencemedia/eslint-config-standard/config/recommended', () => {
  describe('`recommended`', () => it('is an object', () => expect(recommended).to.be.an('object')))
})
