import {
  expect
} from 'chai'

import standard from '@sequencemedia/eslint-config-standard/configs/standard'

describe('@sequencemedia/eslint-config-standard/configs/standard', () => {
  describe('`standard`', () => it('is an object', () => expect(standard).to.be.an('object')))
})
